  Run `npm i` to install the dependencies.
  Run `npm run dev` to start the development server.

  Currently a prototype, shows how all the features will work:
  1. Landing page - sign in with google and choose whether you are a parent/child
  2. ConnectChild - Send an otp to your child's email, verify and link with your account to access the insights.
  